# Issues #61, #15, #62, and #71 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. All work must remain in the single RepoForge workspace created for this batch.

**Goal:** Deliver four currently selectable roadmap slices in one isolated worktree while preserving per-issue boundaries, deterministic contracts, and RepoForge safety invariants.

**Architecture:** Add ticket-governance and risk-recommendation behavior as typed domain/application units, keep GitHub and filesystem behavior behind adapters, and extract durable-state mechanics into a reusable persistence substrate. Remove the stale source manifest in favor of one documented production integrity policy backed by executable regression coverage.

**Tech Stack:** Python 3.10+, frozen dataclasses, enums, Protocol ports, deterministic JSON, pathlib, subprocess with bounded `gh` reads, pytest, Ruff, strict Mypy, shell production gate, RepoForge exact-tree verification.

## Global Constraints

- Work only in the existing `ai/*` RepoForge workspace and never modify `main` directly.
- Preserve all safety invariants in `AGENTS.md` and `SECURITY.md`.
- Do not add third-party dependencies.
- No arbitrary shell, force push, non-draft PR, workflow-file changes, or secret-bearing persistence.
- Every behavior change starts with a failing test and is verified again after implementation.
- Keep public MCP/CLI contracts unchanged except for the documented offline ticket-validation command; public workspace assessment exposure remains #16 scope.
- Keep deterministic ordering and bounded collection sizes.
- Update issue, parent initiative, and program tracking metadata after the draft PR exists.

---

### Task 1: Issue #61 — ticket graph schema, validation, and selection

**Files:**
- Create: `src/repoforge/domain/tickets.py`
- Create: `src/repoforge/application/tickets/__init__.py`
- Create: `src/repoforge/application/tickets/graph.py`
- Create: `src/repoforge/adapters/github/ticket_graph.py`
- Create: `scripts/validate_ticket_graph.py`
- Create: `docs/roadmaps/REPOFORGE_TICKET_GRAPH.json`
- Create: `tests/fixtures/tickets/*.json`
- Create: `tests/test_ticket_graph.py`
- Modify: `scripts/verify-production.sh`
- Modify: `Makefile`
- Modify: `config.repoforge.toml`

**Interfaces:**
- Produces `TicketType`, `TicketPriority`, `TicketStatus`, `TicketNode`, `TicketGraph`, `TicketDiagnostic`, and `TicketSelection`.
- Produces `load_ticket_graph(path)`, `validate_ticket_graph(graph)`, `select_ready_tickets(graph, limit)`, and `compare_live_ticket_metadata(graph, adapter)`.
- The live adapter performs bounded read-only `gh issue view` calls and returns normalized metadata; it never edits GitHub.

- [ ] Write failing parser/schema tests for valid graph, malformed metadata, duplicate IDs, missing parents, unknown blockers, and bounded collection sizes.
- [ ] Run the production gate and confirm the new tests fail because ticket graph modules do not exist.
- [ ] Implement typed enums/models, deterministic JSON parsing, stable diagnostics, and exact allowed-value validation.
- [ ] Run the production gate and confirm parser/schema tests pass.
- [ ] Write failing graph tests for self/circular blockers, asymmetric `blocks`, Ready-with-open-blocker, parent child-index drift, and Backlog exclusion.
- [ ] Implement graph traversal, cycle detection, reciprocal edge checks, parent membership checks, and stable diagnostic ordering.
- [ ] Write failing selection tests proving priority order `P0, P1, P2, P3`, then issue number, with Backlog/Blocked/In-progress/In-review/Done excluded.
- [ ] Implement `select_ready_tickets` with a hard result bound and deterministic output.
- [ ] Write failing live-adapter tests using a fake command executor for bounded reads, title/state/body drift, unavailable `gh`, and no write verbs.
- [ ] Implement the read-only live adapter and normalized drift report.
- [ ] Add the initial complete program manifest and make offline validation pass.
- [ ] Add `scripts/validate_ticket_graph.py` with `--manifest`, `--next`, `--limit`, and optional `--live-repo`; output JSON and exit nonzero on diagnostics.
- [ ] Add `make tickets` and a RepoForge allowlisted `ticket-graph` verification profile.
- [ ] Invoke offline ticket validation from `scripts/verify-production.sh` before lint/type/test/build.
- [ ] Review the exact diff for #61 before proceeding.

### Task 2: Issue #61 — AI-ready issue forms and governance documentation

**Files:**
- Create: `.github/ISSUE_TEMPLATE/initiative.yml`
- Create: `.github/ISSUE_TEMPLATE/implementation-ticket.yml`
- Create: `.github/ISSUE_TEMPLATE/config.yml`
- Create: `docs/development/TICKET_GOVERNANCE.md`
- Modify: `docs/roadmaps/REPOFORGE_MASTER_ROADMAP.md`
- Modify: `README.md`
- Modify: `tests/test_ticket_graph.py`

- [ ] Write failing static tests that require every mandatory form field, allowed metadata options, and a Ready warning that blank detail is invalid.
- [ ] Add the Initiative and Implementation Ticket forms without workflows or automation.
- [ ] Document manifest regeneration, offline/live validation, close semantics, and how authoritative comments override stale title/body metadata.
- [ ] Update roadmap/README references to the governance command and manifest.
- [ ] Run the production gate and review the #61 diff.

### Task 3: Issue #15 — deterministic risk model

**Files:**
- Create: `src/repoforge/domain/risk.py`
- Create: `src/repoforge/application/workspace/risk.py`
- Create: `tests/test_workspace_risk.py`
- Modify: `src/repoforge/domain/assessment.py`
- Modify: `src/repoforge/config.py`
- Modify: `tests/test_config.py`

- [ ] Write failing table-driven tests for docs-only, security/runtime/config/schema, public contract, lockfile, cross-package breadth, budget pressure, stale base, failing CI, missing evidence, stale receipts, and unknown file types.
- [ ] Run the production gate and confirm failure from missing risk APIs.
- [ ] Implement bounded typed risk values, deterministic score contributions, stable evidence references, uncertainty handling, thresholds, and conservative fallback.
- [ ] Write failing recommendation tests for stage ordering, unavailable profiles/diagnostics, broadest critical fallback, retained final profile, manual review, and next-safe-action ordering.
- [ ] Implement policy-driven verification recommendations without executing profiles or issuing receipts.
- [ ] Add strict TOML loading/validation for repository risk policy with safe defaults that retain the configured final verification profile.
- [ ] Run the production gate and review the domain/config diff.

### Task 4: Issue #15 — integrate risk and recommendation into internal assessment

**Files:**
- Modify: `src/repoforge/domain/assessment.py`
- Modify: `src/repoforge/application/workspace/assessment.py`
- Modify: `tests/test_workspace_assessment.py`
- Modify: `docs/roadmaps/REPOFORGE_MASTER_ROADMAP.md`

- [ ] Write failing integration tests for stable serialization, evidence references, low-coverage broadening, critical manual review, docs-only narrow diagnostics with retained final profile, and stale snapshot rejection.
- [ ] Extend assessment validation so risk/recommendation snapshot IDs must match and ordered collections remain bounded.
- [ ] Compute risk and recommendation only after all evidence components are captured and the snapshot is revalidated current.
- [ ] Keep audit metadata limited to workspace ID; do not log changed paths, factors, or recommendation bodies.
- [ ] Run the production gate and review the #15 diff.

### Task 5: Issue #62 — replace stale manifest with executable integrity policy

**Files:**
- Delete: `SOURCE_MANIFEST.sha256`
- Create: `docs/development/INTEGRITY_POLICY.md`
- Create: `tests/test_source_integrity_policy.py`
- Modify: `README.md`
- Modify: `BUILD_REPORT.md`
- Modify: `docs/development/DEVELOPMENT.md`
- Modify: `scripts/verify-production.sh`

- [ ] Write a failing regression test that rejects `SOURCE_MANIFEST.sha256`, stale manifest references, unmanaged hash-list files, and missing integrity-policy documentation.
- [ ] Remove the stale manifest and every reference to it.
- [ ] Document scope, ordering, exclusions, generated artifacts, symlink treatment, line endings, update/review workflow, and failure semantics for the replacement policy.
- [ ] Ensure the production script prints named integrity stages and keeps clean-checkout enforcement.
- [ ] Run the production gate and review the #62 diff.

### Task 6: Issue #71 — shared durable-state domain and port

**Files:**
- Create: `src/repoforge/domain/durable_state.py`
- Create: `src/repoforge/ports/state_repository.py`
- Create: `src/repoforge/adapters/persistence/json_state_repository.py`
- Create: `tests/test_durable_state.py`
- Modify: `src/repoforge/domain/errors.py`
- Modify: `src/repoforge/ports/__init__.py`

- [ ] Write failing tests for deterministic encoding, schema/revision validation, safe collection/record IDs, size bounds, private permissions, atomic cleanup, CAS conflicts, restart reads, corrupt JSON, future schema, identity mismatch, and permission failures.
- [ ] Run the production gate and confirm missing durable-state APIs.
- [ ] Implement typed version/revision/envelope primitives and generic repository protocol.
- [ ] Implement the bounded private JSON repository with stable corruption, permission, persistence, size, stale-revision, and future-version errors.
- [ ] Add safe audit-metadata helpers that expose only record ID, revision, schema version, and action.
- [ ] Run the production gate and review the shared primitive diff.

### Task 7: Issue #71 — adopt shared durable state in OperationTask

**Files:**
- Modify: `src/repoforge/adapters/persistence/json_operation_store.py`
- Modify: `src/repoforge/ports/operation_store.py`
- Modify: `src/repoforge/testing/fakes.py`
- Modify: `tests/test_operation_tasks.py`
- Modify: `tests/test_bootstrap_factories.py`
- Modify: `src/repoforge/bootstrap.py`
- Modify: `docs/roadmaps/REPOFORGE_MASTER_ROADMAP.md`

- [ ] Write failing compatibility tests proving byte-stable operation JSON, unchanged service/MCP results, stale `expected_updated_at` behavior, restart recovery, and exact existing error codes.
- [ ] Refactor `JsonOperationStore` to compose the shared state repository without duplicating write/fsync/permission/CAS logic.
- [ ] Keep operation-specific payload validation and future-schema behavior fail-closed.
- [ ] Update fakes/bootstrap only where typing requires the shared port.
- [ ] Run the production gate and review the #71 diff.

### Task 8: Tracking, final verification, and publication

**Files:**
- Modify: `docs/roadmaps/REPOFORGE_MASTER_ROADMAP.md`
- Modify: `CHANGELOG.md`

- [ ] Review `workspace_diff` and remove unrelated changes.
- [ ] Run `scripts/verify-production.sh --allow-dirty` through the RepoForge `full` profile and require a fresh matching verification receipt.
- [ ] Commit the exact verified tree with a scoped Conventional Commit message summarizing the batch.
- [ ] Push without force and create one draft PR that records risks, compatibility, verification evidence, deferred work, and `Closes #61`, `Closes #15`, `Closes #62`, and `Closes #71`.
- [ ] Update issue #61, #15, #62, and #71 to `In review` with the draft PR link.
- [ ] Update parent initiatives #59, #13, and #70, then update program issue #3 master tracking so the next selectable ticket list is accurate.
- [ ] Read draft PR status/checks and report exact pending/failing/passing state without merging.
